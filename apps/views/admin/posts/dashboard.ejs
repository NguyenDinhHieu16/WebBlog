<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/dashBoard.css">
    <title>Document</title>
</head>
<body>
    <div class="header">
        <div class="MN BlogAdmin">
            <a href="/admin" class="linkTransfer">Blog Admin</a>
        </div>
        <div class="MN posts"><a href="/admin/posts" class="linkTransfer">Posts</a></div>
        <div class="MN Users"><a href="/admin/user" class="linkTransfer">Users</a></div>
    </div>
    <br>
    <div class="row pull-right" >
        <a href="/admin/posts/new" class="btn btn-success">Add new post</a>
    </div>
    <br><br>
    <div id="posts">Posts</div>
    <br>
    <div id="table">
        <table>
            <thead>
                <tr>
                    <td>ID</td>
                    <td>Title</td>
                    <td>Content</td>
                    <td>Author</td>
                    <td>Create Time</td>
                    <td>Update Time</td>
                    <td>Action</td>
                </tr>
            </thead>
            <tbody>
                <% if (data && data.posts) {%>
                    <% for (var i=0; i< data.posts.length; i++) {%>
                        <tr>
                            <td><%= data.posts[i].id %></td>
                            <td><%=data.posts[i].title %></td>
                            <td><%=data.posts[i].content %></td>
                            <td><%=data.posts[i].author %></td>
                            <td><%=data.posts[i].create_at %></td>
                            <td><%=data.posts[i].update_at %></td>
                            <td>
                                <a href="/admin/posts/edit/<%= data.posts[i].id%>" class="btn btn-primary">Edit</a>
                                <button class="btn btn-danger post_delete" post_id="<%= data.posts[i].id%>">Delete</button>
                            </td>
                        </tr>
                    <%}%>
                <%}%>
            </tbody>
        </table>
    </div>
    <script type="text/javascript" src="/static/js/post.js"></script>
</body>
</html>