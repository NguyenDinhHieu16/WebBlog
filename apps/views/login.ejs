<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/login.css">
    <link rel="stylesheet" href="/static/css/font/fontawesome-free-6.4.0-web/css/all.css">
    <script src="/static/js/sign.js"></script>
    <title>Form login unitop.vn</title>
</head>
<body>
    <div id="wrapper">
        <form id="form-login" method="post" action="login">
            <h1 class="form-heading">LOGIN</h1>
            <% if (data && data.error) {%>
            <div id="signUpAlert" class="alert">
                <!-- <p>Error:</p> -->
                <span> <%= data.error %> </span>
            </div>
            <%}%>

            <% if (data && data.message) {%>
                <div id="signUpAlertSuccess" class="alert">
                    <!-- <p>Error:</p> -->
                    <span> <%= data.message %> </span>
                </div>
                <%}%>
            <div class="form-group">
                <i class="far fa-user"></i>
                <input type="text" class="form-input" placeholder="Email" name="email">
            </div>
            <div class="form-group">
                <i class="fas fa-key"></i>
                <input type="password" class="form-input" placeholder="Mật khẩu" id="passW" name="passW">
                <div id="eye">
                    <i class="far fa-eye" onclick="appear()"></i>
                </div>
            </div>
            <input type="submit" value="Đăng nhập" class="form-submit">

            <hr>
            <div class="transfer">
                Don't have an account
                <a href="signUp" class="">SignUp here</a>
            </div>
            
            
        </form>
    </div>
    
</body>
<script src="https://code.jquery.com/jquery-3.6.0.js"></script>
<script src="js/app.js"></script>
</html>